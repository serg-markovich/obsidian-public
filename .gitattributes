# Markdown как UTF-8
*.md      text eol=lf charset=utf-8
*.markdown text eol=lf charset=utf-8

# Изображения в бинарном режиме
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary

# SVG — это XML/текст, не binary!
*.svg  text eol=lf

# LFS для тяжёлых файлов
*.pdf  filter=lfs diff=lfs merge=lfs -text
*.zip  filter=lfs diff=lfs merge=lfs -text
*.mp3  filter=lfs diff=lfs merge=lfs -text
*.mp4  filter=lfs diff=lfs merge=lfs -text

